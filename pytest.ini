[pytest]
markers =
    pg_integration: testes de integracao que exigem um PostgreSQL real (Sprint B, ADR-008). Pulados quando TEST_DATABASE_URL nao esta definida, exceto quando NSI_REQUIRE_PG_TESTS=1 (comando de aceite - falha em vez de pular). Ver tests/conftest.py.
